name: Build Containers (disabled)

# This workflow is superseded by .github/workflows/unified-docker.yml.
# The upstream Containerfiles it referenced were replaced by
# docker/unified/Dockerfile in this fork, so every matrix job failed.
# All automatic triggers (schedule + push) have been removed; only
# manual workflow_dispatch remains as a stub pending full deletion.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled and superseded by unified-docker.yml."
          echo "It will be removed in a follow-up commit."
          exit 0
